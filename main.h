#ifndef MAIN_H_INCLUDED
#define MAIN_H_INCLUDED

#include <stdio.h>
#include <stdlib.h>

#include "mapa.h"
#include "cola.h"

#define TOUP(X) (((X)>='a'&&(X)<='z')?(X)-32:(X))

#endif // MAIN_H_INCLUDED
